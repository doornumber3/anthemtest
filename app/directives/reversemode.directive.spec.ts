import { ReversemodeDirective } from './reversemode.directive';

describe('ReversemodeDirective', () => {
  it('should create an instance', () => {
    const directive = new ReversemodeDirective();
    expect(directive).toBeTruthy();
  });
});
